// Create a folder
// Initialize an node/npm project
// install dotenv as a dev dependency
// Create some environment variables and put them in a .env
// Read them in to the file we are working with
// Checked that they were there! Alert if not. 
// Use them

// import 'dotenv/config'
// console.log(process.env)

console.log(process.env.PORT);
console.log(process.env.USER_NAME);
console.log(process.env.PASSWORD);
